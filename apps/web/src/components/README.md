# Deprecated UI Folder

Do not add new UI to `apps/web/src/components`, except thin web wrappers that
truly belong to the web app.

Shared UI belongs in `packages/design-system`.

See `LEGACY_MIGRATION.md` for owners, replacement targets, and removal stages.
