# Deprecated UI Folder

Do not add new UI to `apps/mobile/src/components`.

This folder is legacy from the pre-shared-component migration. Mobile-specific
behavior that still belongs in the app should live in `apps/mobile/src/bootstrap`
or another clearly platform-specific location.

See `LEGACY_MIGRATION.md` for owners, replacement targets, and removal stages.
