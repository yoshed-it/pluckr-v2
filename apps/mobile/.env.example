# The mobile app reads the shared repo-root `.env.local` during local work.
# Keep the same values in the root file and mirror these names there.

EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key
